#!/usr/bin/env python
# -*- coding: utf-8 -*-
from git import *
from os.path import *

path = dirname(dirname(__file__))
print(path)
print("")
repo = Repo(path)

for r in repo.remotes:
	print("%s %s" % (r.name,r.url))

